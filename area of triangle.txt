#include <stdio.h>
#include <math.h>
int main()
{
int a,b,c;
float s,area;
printf("enter a,b,c values");
scanf("%d%d%d",&a,&b,&c);
S=(a+b+c)/2;
area=sqrt(s*(s-a)*(s-b)*(s-c));
printf("the area is %f", area);
return 0;
}