#include <stdio.h>
int main()
{
int n,a[10],i,large;
printf("enter n value');
scanf("%d",&a[i]);
}
large=a[0];
for(i=1;i<n;i++)
{
printf("enter the value");
scanf("%d",&a[i]);
}
large=a[0];
for (i=1;i<n;i++)
{
if(a[i]>large)
{
large=a[i];
}
}